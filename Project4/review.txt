09/04

1.데이터형식조정에서 매개변수가 있는 조정자와 없는조정자가 나뉜다.

2. 불리언에서 0은 false 0이 아닌 모든수는 true로 인식함


09/07

1. 콤마연산자는 for문이 아니면 사용하기말자. ex) cout << i,j << endl; 이렇게사용 ㄴㄴ

2. 함수(메서드)는 프로그램의 블럭, 작업단위이다.

3. 함수를 만듦으로써 오류검사의 단위가 정해지고, in함수 out함수 process함수로 나눔으로써 가독성이 좋아진다.

4.라이브러리는 함수들이 모여있는 클래스이다.
=======
3. 포인터때문에 ->라는 연산자가 있고 &는 주소연산자이다.

4.자바와는 다르게 delete를 써야한다.


09/08

1. call by value 를 함수에서 기본으로 하는게 좋지만, 여러개의 값을 가져와야되는 경우나 필요한경우에 call by reference를 사용하면좋다.

2. call by reference는 int& 으로 참조값을 가져오고 내보낼수있다. 

3. 상수참조매개변수가 있는데 더빠르게불러올수있다?

4. 오버로딩으로 같은이름의 함수인데 매개변수로 다른함수처럼 사용가능하다.

5. 디폴트인자에서 (int a, int b, int c =1)라면 2개만입력해도 c에는 1이라는 기본적인값이들어간다. 나중꺼부터 디폴트인자를사용해야됨.

09/12

1.재귀에서 마지막으로 호출된 함수부터 처리되는이유는 스택에 겹겹히쌓이다가 젤 위에있는거부터 처리하기때문이다. 

09/14

1. 배열에서 가르키는 거는 &a[0] == a로 쓴다

2. cpp에서 a[6] 이라고하면 a[0]으로 부터 6칸만큼 떨어진 메모리의 칸에 값을 넣기때문에 그공간이 메모리영역인지는 확인하지않아서 위험할수있다. os의 영역을 침범하면 다운될수도있엇다.

3. 배열은 일반 변수로써 선언과 동시에 초기화가능하다. int array[3] = {1,2,3}; 

4. 크기보다 작은 값만 중괄호로 입력하면 나머지값은 0 으로자동초기화된다.

5. 배열의 크기를 지정하지않으면 중괄호안의 요소개수만큼으로 배열의 길이가 정해진다.

6. 배열의 크기를 초기화 할때 숫자를 직접쓰는것 보다 const int num = 3; 으로 상수를 만들고 num을 집어넣는게 유지보수나 코드를 읽는데 좋은방법이다.

7. 자바에서 new로 생성하는 배열과는 다르게 정적배열은 함수의 스택에 고정적인 위치에 만들어지기 때문에 배열 = 배열을 리턴하는함수() 이런식으로 사용이 불가능하다. 

8. 함수에서 배열을 전달할때 배열인자의 크기를 전달하지않아도 실행은 되지만 크기를 옆에 int size로 따로 전달해주고 오류를 발생시켜주는게 필요하다.

9. 정적배열에서 배열의 첫번째 값을 직접바꾸는 함수는 의도한대로 실행되지않는다. call by value때문이다 '

10. 메모리 크기를 컴파일러가 관리해주지않는다 그래서 arr.length를 사용할수없다

09/15

1. 배열 매개변수는 실제로 첫번째 요소의 주소가 전달된다. (참조에 의한 전달과 유사함)

2. 배열안의 내용을 바꾸면안되는 함수에서는 const 함수로 전달해서 배열안의 값을 못바꾸게함

3. 씨플플에서 다차원배열은 자바랑다르게 [30][100] 이라면 3000개의 공간이 메모리 영역에 연달아서 생김 자바는 주소안 주소같은느낌

4. 행우선 저장형식이라서 [0] [1]다음에 [0] [2]가 저장됨.		

5. A[3] 의 주소를 컴퓨터가 사용하는 방식은 * ( a + 3) 이고 *는 주소를 가르키는 방법이다.

6. [2][3][2][2] 라는 배열이 있을때 

09/16

1. 구조체는 public하고 method가 없는 class

2. 자바와 다르게 cpp에서 클래스는 public 영역과 private 영역을 구역별로 정의함

3. 멤버와 멤버함수는 클래스.멤버 로 접근가능하다.

4. ::은 영역지정 연산자이다. 어느 클래스에 있는건지를 알려줌.

5. 캡슐화는 하나로 데이터와	기능을 함께 보낸다는 의미이다. ( 기능을 통해서만 데이터를 바꿀수있게끔함)

6. 클래스는 모든 객체가 접근이가능하다?

7. adt는 내부 코드는 제공하지않고 어떻게 쓰는지만 알려주는 방법이다. 

8. 생성자는 나 자신을 조작하는것이기때문에 리턴값이 따로 필요없다.

9. 생성자는 다른 멤버함수처럼 명시적인 호출할수없다 (.생성자)

10. :초기화 섹션 {} 중괄호가 있어야 함수호출로 인식함.

11. 이미 생성되있는 멤버를 바꾸고싶으면 day1 = DayOfYear(10,31); 과 같이 명시적 생성자호출해서 메모리의 공간을 변경시킨다. 